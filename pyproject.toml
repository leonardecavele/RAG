[project]
name = "RAG"
version = "0.0.1"
description = "Retrieval augmented generation system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"fire>=0.7.1",
	"flake8>=7.3.0",
	"mypy>=1.19.1",
	"pydantic>=2.12.5",
]
